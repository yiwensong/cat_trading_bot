import setuptools

setuptools.setup(
  name='cat_trading_bot',
  version='0.0.0',
  license='Apache',
  packages=setuptools.find_packages(exclude=['tests*']),
  install_requires=[
  ],
  classifiers=[
    'Natural Language :: English',
    'Programming Language :: Python :: 3.6',
  ],
  entry_points={
  },
)
